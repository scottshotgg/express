// This is a file for testing structs

// Define a struct
struct Thing = {
  int fieldA
  string stringField
  bool false_field
  float anotherFielderino
}

float thinger = 0.0

// Instantiate a struct variable
something := Thing{}

// Instantiate a struct variable with some initilizers
something2 := Thing {
  fieldA: 0xDECAF
}

something3 := Thing {
  stringField: "woah its me"

  // You can declare local variables within structs however, 
  // unlike objects, they will not get carried out
  not_in_this_struct := "wtf dude"
}

Print(something)
